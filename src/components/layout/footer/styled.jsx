import styled from "styled-components";

const StyledFooter = styled.footer`
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 24px 86px;
  border-top: 1px solid rgba(0, 0, 0, 0.1);
`;

const StyledFooterText = styled`
    font-weight: 400;
    font-size: 18px;
    line-height: 27px;
`;

export { StyledFooter, StyledFooterText };
