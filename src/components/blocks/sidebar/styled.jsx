import styled from "styled-components";

export const StyledSideBar = styled.aside`
  grid-area: 1 / 1 / 2 / 2;

  display: flex;
  flex-direction: column;
  gap: 18px;

  position: sticky;
  top: 90px;
`;
